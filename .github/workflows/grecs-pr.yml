name: GRECS PR

on:
  pull_request_target:
    types:
      - opened
      - reopened
      - edited
      - labeled
      - unlabeled
      - closed

permissions:
  contents: read
  pull-requests: write

jobs:
  grecs-pr:
    uses: nexthop-ai/nh/.github/workflows/grecs-pr.reusable.yml@main
    secrets: inherit
